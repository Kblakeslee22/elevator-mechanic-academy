<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">

  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  >

  <title>Hydraulic Force Lab | EMA</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 32px 16px;
      font-family: Arial, Helvetica, sans-serif;
      background: #111820;
      color: #ffffff;
    }

    .lab {
      width: min(760px, 100%);
      margin: 0 auto;
    }

    .eyebrow {
      margin-bottom: 6px;
      color: #d8a928;
      font-size: 0.85rem;
      font-weight: bold;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    h1 {
      margin-top: 0;
    }

    .intro {
      line-height: 1.6;
      color: #d9e0e6;
    }

    .formula {
      margin: 24px 0;
      padding: 18px;
      background: #18232e;
      border-left: 5px solid #d8a928;
      border-radius: 6px;
      text-align: center;
      font-size: 1.4rem;
      font-weight: bold;
    }

    .calculator {
      padding: 28px;
      background: #ffffff;
      color: #18232e;
      border-radius: 14px;
    }

    .field {
      margin-bottom: 20px;
    }

    label {
      display: block;
      margin-bottom: 7px;
      font-weight: bold;
    }

    input {
      width: 100%;
      padding: 12px;
      border: 1px solid #9da9b3;
      border-radius: 6px;
      font-size: 1rem;
    }

    button {
      width: 100%;
      padding: 14px;
      border: 0;
      border-radius: 7px;
      background: #d8a928;
      color: #111820;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
    }

    button:hover {
      filter: brightness(1.05);
    }

    .results {
      margin-top: 24px;
      padding: 20px;
      background: #eef2f5;
      border-radius: 8px;
    }

    .result-row {
      display: flex;
      justify-content: space-between;
      gap: 20px;
      padding: 10px 0;
      border-bottom: 1px solid #ccd4da;
    }

    .result-row:last-child {
      border-bottom: 0;
    }

    .result-value {
      font-weight: bold;
    }

    .feedback {
      margin-top: 18px;
      line-height: 1.5;
    }

    .error {
      color: #a60000;
      font-weight: bold;
    }

    .back-link {
      display: inline-block;
      margin-top: 24px;
      color: #d8a928;
    }
  </style>
</head>

<body>
  <main class="lab">
    <p class="eyebrow">EMA Interactive Lab</p>

    <h1>Hydraulic Force and Pascal's Law</h1>

    <p class="intro">
      Adjust the values below to see how hydraulic pressure
      transfers force from one piston to another.
    </p>

    <div class="formula" aria-label="Pressure equals force divided by area">
      Pressure = Force ÷ Area
    </div>

    <section class="calculator">
      <div class="field">
        <label for="inputForce">
          Input Force, F₁ (pounds)
        </label>

        <input
          id="inputForce"
          type="number"
          min="0"
          step="any"
          value="500"
        >
      </div>

      <div class="field">
        <label for="inputArea">
          Input Piston Area, A₁ (square inches)
        </label>

        <input
          id="inputArea"
          type="number"
          min="0.01"
          step="any"
          value="5"
        >
      </div>

      <div class="field">
        <label for="outputArea">
          Output Piston Area, A₂ (square inches)
        </label>

        <input
          id="outputArea"
          type="number"
          min="0.01"
          step="any"
          value="80"
        >
      </div>

      <button type="button" id="calculateButton">
        Calculate Hydraulic Output
      </button>

      <div
        class="results"
        id="results"
        aria-live="polite"
      >
        <div class="result-row">
          <span>System pressure</span>
          <span class="result-value" id="pressureResult">
            100 psi
          </span>
        </div>

        <div class="result-row">
          <span>Output force</span>
          <span class="result-value" id="outputForceResult">
            8,000 lb
          </span>
        </div>

        <div class="result-row">
          <span>Ideal mechanical advantage</span>
          <span class="result-value" id="advantageResult">
            16:1
          </span>
        </div>

        <p class="feedback" id="feedback">
          The larger output piston produces 16 times the input
          force under ideal conditions.
        </p>
      </div>
    </section>

    <a class="back-link" href="../index.html">
      ← Return to Interactive Labs
    </a>
  </main>

  <script>
    "use strict";

    const inputForce = document.getElementById("inputForce");
    const inputArea = document.getElementById("inputArea");
    const outputArea = document.getElementById("outputArea");
    const calculateButton =
      document.getElementById("calculateButton");

    const pressureResult =
      document.getElementById("pressureResult");

    const outputForceResult =
      document.getElementById("outputForceResult");

    const advantageResult =
      document.getElementById("advantageResult");

    const feedback = document.getElementById("feedback");

    function formatNumber(value, decimals = 2) {
      return value.toLocaleString("en-US", {
        maximumFractionDigits: decimals
      });
    }

    function calculateHydraulics() {
      const force = Number(inputForce.value);
      const areaOne = Number(inputArea.value);
      const areaTwo = Number(outputArea.value);

      const valuesAreValid =
        Number.isFinite(force) &&
        Number.isFinite(areaOne) &&
        Number.isFinite(areaTwo) &&
        force >= 0 &&
        areaOne > 0 &&
        areaTwo > 0;

      if (!valuesAreValid) {
        pressureResult.textContent = "—";
        outputForceResult.textContent = "—";
        advantageResult.textContent = "—";

        feedback.innerHTML =
          '<span class="error">' +
          "Enter valid positive piston areas and a force " +
          "of zero or greater." +
          "</span>";

        return;
      }

      const pressure = force / areaOne;
      const outputForce = pressure * areaTwo;
      const mechanicalAdvantage = areaTwo / areaOne;

      pressureResult.textContent =
        `${formatNumber(pressure)} psi`;

      outputForceResult.textContent =
        `${formatNumber(outputForce)} lb`;

      advantageResult.textContent =
        `${formatNumber(mechanicalAdvantage)}:1`;

      if (mechanicalAdvantage > 1) {
        feedback.textContent =
          `The output piston produces approximately ` +
          `${formatNumber(mechanicalAdvantage)} times the ` +
          `input force under ideal conditions.`;
      } else if (mechanicalAdvantage === 1) {
        feedback.textContent =
          "Equal piston areas produce equal input and output " +
          "forces under ideal conditions.";
      } else {
        feedback.textContent =
          "Because the output piston is smaller, output force " +
          "is reduced while output travel increases.";
      }
    }

    calculateButton.addEventListener(
      "click",
      calculateHydraulics
    );

    [inputForce, inputArea, outputArea].forEach((input) => {
      input.addEventListener("input", calculateHydraulics);
    });

    calculateHydraulics();
  </script>
</body>
</html>
